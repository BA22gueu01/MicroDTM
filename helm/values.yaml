# Default values for sock-shop.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
global:
    registry: ''

istio:
    enabled: false

trustcalc:
    replicas: 1
    image:
        repo: vogtrap1/ba22_gueu_01
        tag: latest
    resources:
        limits:
            cpu: 300m
            memory: 1000Mi
        requests:
            cpu: 100m
            memory: 300Mi

